# Exclude the .data directory, which contains the data as can be seen on
# Kaggle. Files related to the dataset but generated by ourselves
# (i.e. trainSet.csv) are not excluded.
data

# Ignore the checkpoints, since git already takes care of versioning.
.ipynb_checkpoints

# Do not include the virtual environment.
.venv

# Do not include the logs to tensorboard.
runs

# Do not include Python cache.
__pycache__/
